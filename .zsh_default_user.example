#!/usr/bin/env zsh
# vim: awa:sts=4:ts=4:sw=4:et:cin:fdm=manual:tw=120:ft=zsh

# Since each profile may have a different name take this example file, copy it to "user-default.zsh"
#  and create the symbolic link in the $HOME directory.
export DEFAULT_USER="jasoni"

